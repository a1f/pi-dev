# Keep the build context tiny — the runtime gets everything via the bind mount.
# `container/` IS needed in the build context (COPY'd into the image).
.git
.gitignore
.env
.env.*
node_modules/
.DS_Store
*.log
*.md
LICENSE
docker-compose.yml
scripts/
.pi/
extensions/
